# =============================================================================
# requirements-test.txt — 테스트 실행에 필요한 패키지
#
# 설치: pip install -r requirements-test.txt
# 전체 테스트 실행: pytest tests/e2e/ -m "not integration"
# 통합 테스트 포함: pytest tests/e2e/ --timeout=300
# =============================================================================

# 테스트 프레임워크
pytest>=7.0
pytest-asyncio>=0.21
pytest-mock>=3.0

# 타임아웃 제어 (통합 테스트용)
pytest-timeout>=2.1

# 타입 검사 (선택)
# mypy>=1.10

# 커버리지 리포트 (선택)
# pytest-cov>=4.0
