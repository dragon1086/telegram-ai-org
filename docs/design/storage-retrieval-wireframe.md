# 저장·조회 흐름 와이어프레임

**문서 ID**: DESIGN-2026-03-22-WIRE-001
**작성일**: 2026-03-22
**작성자**: aiorg_design_bot
**버전**: v1.0
**상태**: 완성본

---

## 범례 (Legend)

```
┌─────────────────────────────────────────────────────────────────┐
│ 노드 표기 규칙                                                    │
├──────────────────────┬──────────────────────────────────────────┤
│ [  TEXT  ]           │ 시작/종료 노드 (둥근 사각형)               │
│ ──── TEXT ────       │ 프로세스 단계 (직사각형)                    │
│ <  TEXT  >           │ 분기 조건 (다이아몬드)                     │
│ ((  TEXT  ))         │ 데이터 저장소 (원통형)                     │
│ ═══ TEXT ═══         │ 화살표 레이블                              │
│ ├── 번호             │ 병렬 처리 분기                             │
│ ⚡ 아이콘            │ 자동화 단계 (LLM 호출)                     │
│ ⚠️ 아이콘            │ 조건부/주의 단계                           │
│ ✅ 아이콘            │ 성공 종료                                  │
│ ❌ 아이콘            │ 폴백/에러 처리                             │
└──────────────────────┴──────────────────────────────────────────┘

색상 코드 (텍스트 표기):
  [STORE]  = 저장 흐름 (#2980B9 파란색)
  [QUERY]  = 조회 흐름 (#27AE60 초록색)
  [LLM]    = LLM 처리 (#F39C12 앰버)
  [SYS]    = 시스템 자동 (#8E44AD 퍼플)
  [USER]   = 사용자/봇 입력 (#E74C3C 레드)
```

---

# PAGE 1: 저장 흐름 (Storage Flow)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                     📥 메모리 저장 흐름
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[USER] 이벤트 발생
   │
   │   이벤트 유형:
   │   • 태스크 완료 (task_done 이벤트)
   │   • 중요 결정 발생 (decision 키워드 감지)
   │   • 피드백 수신 (feedback 태그 포함)
   │   • 수동 기록 요청 (Rocky 직접 지시)
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 1 [SYS]: 이벤트 캡처 & 원문 추출                       │
│ • 대화 컨텍스트 수집 (최근 N턴)                              │
│ • 이벤트 메타 (발생 조직, 태스크 ID, 타임스탬프) 수집       │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
                   < importance 사전 추정 >
                   (키워드 휴리스틱 기반)
                  /                         \
           [ ≥ 6 ]                       [ < 6 ]
               │                              │
               ▼                              ▼
               │                    ⚠️ 저장 건너뜀
               │                    (임시/일회성 판단)
               │                     → 로그만 기록
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 2 [LLM]: ⚡ 경량 LLM 호출 (Gemini Flash)               │
│                                                             │
│  입력:                                                       │
│  • 원문 컨텍스트                                             │
│  • 이벤트 메타                                               │
│  • 프롬프트: "아래 내용에서 기억할 사실을 추출하고           │
│    YAML frontmatter 형식으로 반환하라"                       │
│                                                             │
│  출력 (자동 생성 필드):                                      │
│  ├─ type        (decision|fact|task|...)                    │
│  ├─ domain      (engineering|design|...)                    │
│  ├─ importance  (0-10 정수)                                 │
│  ├─ summary     (1-2줄 요약)                                │
│  ├─ keywords    (핵심 키워드 배열)                          │
│  ├─ tags        (계층형 태그 배열)                          │
│  └─ related[]   (연관 메모리/태스크 매핑)                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
                 < LLM 호출 성공 여부? >
                /                         \
          [성공]                          [실패]
              │                               │
              ▼                               ▼
              │                     ❌ 폴백 처리
              │                     • BM25 키워드만 추출
              │                     • importance = 5 기본값
              │                     • 수동 메타데이터 입력 요청
              │                               │
              └──────────────┬────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 3 [SYS]: YAML Frontmatter 조립                         │
│                                                             │
│  시스템 자동 채움:                                           │
│  ├─ id:         "mem-{YYYY-MM-DD}-{auto_seq}"              │
│  ├─ version:    "1.0"                                       │
│  ├─ status:     "active"                                   │
│  ├─ created_at: (현재 ISO 8601)                             │
│  ├─ updated_at: (현재 ISO 8601)                             │
│  ├─ author:     (호출한 봇 ID)                              │
│  └─ scope:      (LLM 제안 → 시스템 검증)                   │
│                                                             │
│  LLM 생성값 병합:                                            │
│  type, domain, importance, summary, keywords, tags, related │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
              < importance ≥ 9 AND Rocky 검토 필요? >
              /                                       \
          [Yes]                                     [No]
              │                                         │
              ▼                                         │
┌─────────────────────────┐                             │
│ STEP 3a [USER]: 알림    │                             │
│ • Telegram으로 Rocky에  │                             │
│   검토 요청 알림 발송   │                             │
│ • status = "pending"으로│                             │
│   임시 저장             │                             │
└────────────┬────────────┘                             │
             │ (Rocky 승인 후)                          │
             │ approved_by = "rocky"                   │
             │ status = "active"                        │
             └──────────────────┬───────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 4 [SYS]: 마크다운 파일(.md) 저장                       │
│                                                             │
│  파일 경로: memory/{id}.md                                  │
│  구조:                                                       │
│  ┌─────────────────────────────────┐                        │
│  │ ---                             │                        │
│  │ id: "mem-2026-03-22-001"        │   ← YAML frontmatter  │
│  │ type: "decision"                │                        │
│  │ ...                             │                        │
│  │ ---                             │                        │
│  │                                 │                        │
│  │ # 제목                          │   ← 본문 (마크다운)    │
│  │ 내용...                         │                        │
│  └─────────────────────────────────┘                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
         ┌─────────────────┼──────────────────────┐
         │                 │                      │
         ▼                 ▼                      ▼
┌────────────────┐ ┌────────────────┐ ┌──────────────────────┐
│ STEP 5a [SYS] │ │ STEP 5b [SYS] │ │  STEP 5c [SYS]       │
│ SQLite 인덱스  │ │ 관계 테이블    │ │  [Phase 2] Chroma    │
│ 업데이트       │ │ 업데이트       │ │  임베딩 비동기 큐     │
│               │ │               │ │  enqueue              │
│ memories 테이 │ │ memory_relat  │ │                      │
│ 블 INSERT/    │ │ ions 테이블   │ │                      │
│ UPDATE        │ │ INSERT        │ │                      │
└───────┬────────┘ └───────┬────────┘ └──────────┬───────────┘
        │                  │                      │
        └──────────────────┴──────────────────────┘
                           │
                           ▼
                   ✅ 저장 완료
                   • 저장된 memory_id 반환
                   • MEMORY.md 인덱스 갱신
```

---

# PAGE 2: 조회 흐름 (Retrieval Flow)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                     🔍 메모리 조회 흐름
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[USER] 쿼리 입력
   │
   │   쿼리 유형 예시:
   │   • "ACT-5 완료일이 언제야?" (정확한 사실)
   │   • "캐시 관련 결정사항 알려줘" (시맨틱)
   │   • "T-260 태스크에서 파생된 항목들" (관계 탐색)
   │   • "최근 엔지니어링 결정 5개" (필터+정렬)
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 1 [LLM]: ⚡ 쿼리 분석 & 라우팅                         │
│                                                             │
│  입력: 자연어 쿼리                                           │
│                                                             │
│  판단 기준:                                                  │
│  • 고유 ID/날짜 포함? → EXACT 라우트                        │
│  • 의미론적 유사도 필요? → SEMANTIC 라우트                  │
│  • "에서 파생", "관련된" 키워드? → RELATIONAL 라우트        │
│  • 복합 조건? → HYBRID 라우트                               │
└──────────────────────────┬──────────────────────────────────┘
                           │
          ┌────────────────┼──────────────────────────┐
          │                │                          │
          ▼                ▼                          ▼
   [EXACT 라우트]   [SEMANTIC 라우트]         [RELATIONAL 라우트]
   정확 사실 조회    의미론적 유사 조회         관계 탐색 조회


═══════════════════════════════════════════════════════════════
  분기 A: EXACT 라우트
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│ STEP 2A [SYS]: SQLite 메타 인덱스 조회                      │
│                                                             │
│  필터 우선순위:                                              │
│  1. status = 'active' (필수 선행 조건)                      │
│  2. valid_until IS NULL OR valid_until >= TODAY             │
│  3. 추가 필터 (type, domain, scope, importance 범위 등)     │
│                                                             │
│  인덱스 사용:                                                │
│  └─ idx_status_importance → idx_type_domain → idx_created_at│
│                                                             │
│  결과: memory_id 목록 반환                                   │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
   < 결과 있음? >
  /              \
[Yes]           [No]
  │               └─→ ❌ "관련 메모리 없음" 반환
  ▼
[D 단계로 이동 → 결과 병합]


═══════════════════════════════════════════════════════════════
  분기 B: SEMANTIC 라우트
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│ STEP 2B-1 [SYS]: BM25 키워드 검색 (즉시 가능)              │
│                                                             │
│  • FTS5 virtual table on (summary, keywords)               │
│  • 쿼리 토큰 → BM25 스코어링 → 상위 20개                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
             < Phase 2 임베딩 활성화 여부? >
            /                               \
         [Yes]                            [No]
            │                               │
            ▼                               │
┌─────────────────────────┐                 │
│ STEP 2B-2 [SYS]:        │                 │
│ Chroma 벡터 검색        │                 │
│                         │                 │
│ 1. 쿼리 텍스트 임베딩   │                 │
│ 2. 코사인 유사도 Top-K  │                 │
│ 3. embedding_updated_at │                 │
│    NULL 항목 제외       │                 │
└────────────┬────────────┘                 │
             │                              │
             └──────────────┬───────────────┘
                            │
                            ▼
                 [BM25 결과 + 벡터 결과 병합]
                 • 중복 제거 (memory_id 기준)
                 • importance 가중 재정렬
                            │
                            ▼
               [D 단계로 이동 → 결과 병합]


═══════════════════════════════════════════════════════════════
  분기 C: RELATIONAL 라우트
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│ STEP 2C [SYS]: 관계 인덱스 탐색 (1-hop)                    │
│                                                             │
│  입력: 기준 ID (task-T-xxx 또는 mem-xxxx)                  │
│                                                             │
│  쿼리:                                                       │
│  SELECT * FROM memory_relations                             │
│  WHERE to_id = :target_id                                   │
│    AND confidence >= 0.5                                    │
│  ORDER BY confidence DESC                                   │
│                                                             │
│  인덱스: idx_relations_to_id                                │
└──────────────────────────┬──────────────────────────────────┘
                           │
             < N-hop 탐색 필요? (Q-012) >
            /                             \
        [Yes, Phase 2+]               [No, 1-hop]
            │                               │
            ▼                               │
┌─────────────────────────┐                 │
│ ⚠️ N-hop 재귀 탐색       │                 │
│ (Phase 2 이후 구현)      │                 │
│ 최대 depth 제한 적용     │                 │
└────────────┬────────────┘                 │
             │                              │
             └──────────────┬───────────────┘
                            │
               [D 단계로 이동 → 결과 병합]


═══════════════════════════════════════════════════════════════
  D: 결과 병합 & 후처리 (공통)
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│ STEP 3 [SYS]: 유효성 체크 & 필터링                         │
│                                                             │
│  • valid_until 초과 항목 제외                               │
│  • status = 'active' 재확인                                 │
│  • confidence < 0.5 관계 제외 (관계 탐색 시)               │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 4 [SYS]: 정렬 & 상위 K개 선택                         │
│                                                             │
│  정렬 우선순위:                                              │
│  1순위: importance DESC                                     │
│  2순위: created_at DESC (최신 우선)                         │
│  3순위: confidence DESC (관계 탐색 시)                      │
│                                                             │
│  K 결정 기준:                                                │
│  • 기본: 상위 10개                                          │
│  • context window 제한 내 최대                              │
│  • HYBRID 라우트: 상위 20개 → 재랭킹 → 상위 10개           │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 5 [SYS]: 마크다운 파일 로드 & 컨텍스트 조립           │
│                                                             │
│  1. memory_id → file_path 매핑 (MEMORY 테이블)             │
│  2. .md 파일 읽기 (frontmatter + 본문)                      │
│  3. 컨텍스트 블록 구성:                                      │
│     ┌─────────────────────────────┐                         │
│     │ [MEM-001] type: decision    │                         │
│     │ importance: 9               │                         │
│     │ summary: "캐시 레이어 도입" │                         │
│     │ related: task-T-260         │                         │
│     │ ---                         │                         │
│     │ 본문 내용...                │                         │
│     └─────────────────────────────┘                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
                   ✅ 조회 완료
                   • 컨텍스트 블록 반환
                   • context window에 주입
                   • LLM이 활용하여 응답 생성
```

---

# PAGE 3: 크론 자동화 흐름 (Cron Automation Flow)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                  ⏰ 자동화 유지보수 흐름
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[SYS] 매일 자정 크론 실행
   │
   ├── JOB-1: 만료 항목 처리
   │     │
   │     ▼
   │   SELECT id FROM memories
   │   WHERE valid_until < TODAY AND status = 'active'
   │     │
   │     ▼
   │   status → 'deprecated' 일괄 업데이트
   │     │
   │     ▼
   │   ⚠️ Rocky에게 만료 목록 Telegram 알림 발송
   │
   ├── JOB-2: 인덱스-파일 동기화 감사 (매 1시간)
   │     │
   │     ▼
   │   memory/ 디렉토리 파일 목록 수집
   │     │
   │     ▼
   │   < SQLite에 없는 파일 있음? >
   │   /                         \
   │ [Yes]                     [No]
   │   │                          │
   │   ▼                          ▼
   │ 누락 파일 재인덱싱           ✅ 동기화 정상
   │ (frontmatter 파싱 → 삽입)
   │
   └── JOB-3: [Phase 2] 임베딩 백로그 처리
         │
         ▼
       embedding_updated_at IS NULL 항목 조회
         │
         ▼
       배치 임베딩 생성 (Chroma enqueue)
         │
         ▼
       embedding_updated_at 갱신
```

---

*이전 문서: [metadata-hierarchy-diagram.md](metadata-hierarchy-diagram.md)*
*다음 문서: [design-color-legend.md](design-color-legend.md)*
