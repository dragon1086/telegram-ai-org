# =============================================================================
# telegram-ai-org 환경변수 설정 파일
# 이 파일을 .env 로 복사한 뒤 각 값을 채워주세요:
#   cp .env.example .env
#
# 오픈소스 원클릭 설치:
#   bash scripts/setup.sh          # 엔진 자동 감지 + 경로 자동 설정
#   bash scripts/setup.sh --yes    # 비대화형 자동 설치
#
# 엔진 변경: 설치 후 텔레그램 방에서 /set_engine <engine> 명령어 사용
#   예: /set_engine claude-code | /set_engine codex | /set_engine gemini-cli
# =============================================================================

# =============================================================================
# [필수] 텔레그램 봇 토큰
# @BotFather 에서 봇을 생성하여 발급받은 토큰
# =============================================================================

# PM 글로벌 봇 토큰 (필수) — docker-compose / PyPI 배포 표준 변수명
TELEGRAM_BOT_TOKEN=
# 하위 호환: 기존 설정 유지 (PM_BOT_TOKEN 과 동일)
PM_BOT_TOKEN=

# 그룹 채팅 ID — 음수값, 예: -5203707291 (필수)
# 방법: 그룹에 @userinfobot 초대 후 메시지 전송
TELEGRAM_GROUP_CHAT_ID=

# 관리자 개인 채팅 ID — 봇 시작/오류 알림 수신 (선택, 권장)
# 방법: @userinfobot 에게 DM → "Your ID: xxxxxxx"
# macOS 권한 팝업 사전 알림에 사용됨 (scripts/start_all.sh)
ADMIN_CHAT_ID=

# 부서 봇 토큰 (필수 — 해당 부서를 사용할 경우)
BOT_TOKEN_AIORG_PRODUCT_BOT=      # 기획실
BOT_TOKEN_AIORG_ENGINEERING_BOT=  # 개발실
BOT_TOKEN_AIORG_DESIGN_BOT=       # 디자인실 (claude-code 엔진)
BOT_TOKEN_AIORG_GROWTH_BOT=       # 성장실 (gemini-cli 엔진)
BOT_TOKEN_AIORG_OPS_BOT=          # 운영실 (gemini-cli 엔진, codex→gemini 전환 2026-03-26)
BOT_TOKEN_AIORG_RESEARCH_BOT=     # 리서치실 (gemini-cli 엔진)

# 추가 팀 PM 토큰 (선택 — 별도 팀 구성 시)
MKT_PM_TOKEN=   # 마케팅 팀 PM 봇 토큰
DEV_PM_TOKEN=   # 개발 팀 PM 봇 토큰

# Watchdog 전용 봇 토큰 (선택 — PM_BOT_TOKEN 대체)
WATCHDOG_BOT_TOKEN=

# Watchdog 전용 채팅 ID (선택 — TELEGRAM_GROUP_CHAT_ID 대체)
WATCHDOG_CHAT_ID=

# 부서 봇 개인/그룹 채팅 ID (선택 — COLLAB 메시지 수신 및 배분에 필요)
# 방법: 각 부서 봇을 그룹에 초대 후 @userinfobot 으로 확인
ENGINEERING_BOT_CHAT_ID=   # 개발실 봇 채팅 ID (collab_dispatcher.py 참조)
DESIGN_BOT_CHAT_ID=        # 디자인실 봇 채팅 ID (collab_dispatcher.py 참조)
PRODUCT_BOT_CHAT_ID=       # 기획실 봇 채팅 ID (collab_dispatcher.py 참조)
GROWTH_BOT_CHAT_ID=        # 성장실 봇 채팅 ID (collab_dispatcher.py 참조)
OPS_BOT_CHAT_ID=           # 운영실 봇 채팅 ID (collab_dispatcher.py 참조)
RESEARCH_BOT_CHAT_ID=      # 리서치실 봇 채팅 ID (collab_dispatcher.py 참조)

# =============================================================================
# [필수] API 키 — LLM Confidence Scoring 및 엔진용
# 우선순위: GEMINI_API_KEY → ANTHROPIC_API_KEY → DEEPSEEK_API_KEY → keyword fallback
# OPENAI_API_KEY는 제거됨 — Codex CLI는 ~/.codex/auth.json OAuth 사용
# =============================================================================

# Google Gemini API 키 (권장 — gemini-2.5-flash 모델 사용)
GEMINI_API_KEY=
GOOGLE_API_KEY=   # GEMINI_API_KEY의 대체 키 (선택)

# Anthropic REST API 키 (선택 — GEMINI 없을 때 대체)
ANTHROPIC_API_KEY=

# DeepSeek API 키 (선택 — 최하위 대체)
DEEPSEEK_API_KEY=

# =============================================================================
# [엔진] === Claude Code ===
# claude CLI 기반 실행 엔진 (개발실·기획실·PM 기본 엔진)
# 인증: 최초 실행 시 브라우저 OAuth 자동 진행
# =============================================================================

# Claude Code OAuth 토큰 (필수 — claude CLI 인증용)
# 발급: claude 명령 실행 후 브라우저 로그인
CLAUDE_CODE_OAUTH_TOKEN=

# claude CLI 실행 파일 경로 (선택 — setup.sh 가 자동 감지)
# 기본값: /Users/rocky/.local/bin/claude
CLAUDE_CLI_PATH=

# Claude Code 태스크 기본 타임아웃 (선택, 초 단위, 기본값: 14400 = 4시간)
CLAUDE_DEFAULT_TIMEOUT_SEC=14400

# 실험적 에이전트 팀 기능 활성화 (선택, 1=활성화)
CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=0

# =============================================================================
# [엔진] === Codex ===
# OpenAI Codex CLI 기반 실행 엔진 (레거시 — 현재 모든 조직은 gemini-cli 또는 claude-code 사용)
# 인증 우선순위: ~/.codex/auth.json (OAuth) → OPENAI_API_KEY (API 키)
# OAuth 인증: codex login 실행 후 브라우저 인증 (권장)
# =============================================================================

# OpenAI API 키 (선택 — OAuth 없을 때 대체 인증)
# OAuth 인증이 완료된 경우 불필요 (~/.codex/auth.json 존재 시)
OPENAI_API_KEY=

# codex CLI 실행 파일 경로 (선택 — setup.sh 가 자동 감지)
# 기본값: /opt/homebrew/bin/codex
CODEX_CLI_PATH=

# Codex 기본 타임아웃 (선택, 초 단위, 기본값: 1800 = 30분)
CODEX_DEFAULT_TIMEOUT_SEC=1800

# Codex 복잡 태스크 타임아웃 (선택, 초 단위, 기본값: 14400 = 4시간)
CODEX_COMPLEX_TIMEOUT_SEC=14400

# Codex 리포지토리 검색 루트 경로 (선택, 콜론으로 구분)
CODEX_REPO_SEARCH_ROOTS=

# =============================================================================
# [엔진] === Gemini CLI ===
# Google Gemini CLI 기반 실행 엔진 (성장실·리서치실 기본 엔진)
# 인증: OAuth 2.0 — gemini auth login 실행 → ~/.gemini/oauth_creds.json 생성
# API Key 인증은 지원하지 않음 (OAuth 전용)
# =============================================================================

# gemini CLI 실행 파일 경로 (선택 — setup.sh 가 자동 감지 후 기재)
# Homebrew 설치: brew install gemini-cli → /opt/homebrew/bin/gemini
# npm 설치:      npm install -g @google/gemini-cli → ~/.local/bin/gemini
# 비워두면 setup.sh 실행 시 자동 탐색 후 기재됩니다
GEMINI_CLI_PATH=

# Gemini CLI OAuth 인증 파일 경로 (선택 — 기본값: ~/.gemini/oauth_creds.json)
# gemini auth login 실행 시 자동 생성됨
# CI 환경에서는 GEMINI_OAUTH_CREDS GitHub Secret → 이 경로에 복원
GEMINI_OAUTH_CREDS_PATH=~/.gemini/oauth_creds.json

# Gemini CLI 기본 타임아웃 (선택, 초 단위, 기본값: 1800 = 30분)
# 긴 리서치 태스크에 대응하기 위해 30분으로 설정
GEMINI_CLI_DEFAULT_TIMEOUT_SEC=1800

# Gemini CLI 기본 모델 (선택, 기본값: gemini-2.5-flash)
# 주의: gemini-2.0-flash는 2026-06-01 서비스 종료 예정 — 사용 금지
# 주의: gemini-3.x는 Preview 단계 — 프로덕션 사용 자제
GEMINI_CLI_MODEL=gemini-2.5-flash

# Gemini 폴백 모델: Preview 모델 실패 시 자동 전환 (GA stable)
# Preview 모델(예: gemini-2.5-flash-preview-image-generation) 호출 실패 시
# GeminiCLIRunner가 이 모델로 자동 재시도한다.
GEMINI_FALLBACK_MODEL=gemini-2.5-flash

# =============================================================================
# [엔진 선택] 초기 기본 엔진
# ⚠️  DEPRECATED: 엔진은 /set_engine 봇 명령어로 관리 (organizations.yaml 기준)
#     이 변수들은 setup.sh 초기 설치 시 1회 자동 기재되지만,
#     런타임 봇은 organizations.yaml의 preferred_engine 필드를 우선 참조합니다.
#     엔진 변경: 텔레그램 방에서 /set_engine claude-code | codex | gemini-cli
# =============================================================================

# 표준 엔진 변수 (setup.sh 자동 감지 후 기재 — 런타임 우선 참조)
# 선택 가능한 값: claude-code | codex | gemini-cli
# setup.sh가 감지된 엔진을 자동으로 기재합니다
AI_ENGINE=

# [DEPRECATED] setup.sh 초기 설치 시 자동 기재 — 런타임 참조 변수 아님
ENGINE=

# [DEPRECATED] setup.sh 초기 설치 시 자동 기재 — 런타임 참조 변수 아님
ACTIVE_ENGINE=

# 기본 엔진 (setup.sh 자동 감지 후 기재 — 런타임 기본값으로 사용)
# 선택 가능한 값: claude-code | codex | gemini-cli
DEFAULT_ENGINE=

# docker-compose ENGINE_TYPE 환경변수 (docker-compose.yml 표준 변수명)
# 선택 가능한 값: claude-code | codex | gemini-cli
# docker compose up 시 각 조직 서비스가 docker-compose.yml에서 덮어씀
ENGINE_TYPE=

# =============================================================================
# [Docker] Redis 및 컨테이너 연결 설정
# docker compose up 시 사용되는 서비스 연결 주소
# =============================================================================

# Redis 연결 주소 (선택, 기본값: redis://localhost:6379)
# Docker 환경: redis://aiorg-redis:6379
# 로컬 환경:   redis://localhost:6379
REDIS_URL=redis://localhost:6379

# Docker 컨테이너 내부 데이터 경로 (docker-compose.yml volumes 마운트 기준)
# 로컬 개발 시에는 ~/.ai-org/ 기준 사용
DOCKER_DATA_DIR=/app/data

# =============================================================================
# [CI/CD] GitHub Actions / release pipeline
# GitHub Repository Secrets 와 동일한 이름으로 관리
# =============================================================================

# PyPI publish token (Repository secret: PYPI_API_TOKEN)
PYPI_API_TOKEN=

# Docker Hub username / access token (Repository secrets: DOCKER_USERNAME / DOCKER_PASSWORD)
DOCKER_USERNAME=
DOCKER_PASSWORD=

# =============================================================================
# [기능 플래그] Feature Flags
# 0=비활성화, 1=활성화
# =============================================================================

# PM 중앙 통제 모드 (1: PM 봇이 모든 태스크를 직접 디스패치)
ENABLE_PM_ORCHESTRATOR=1

# 부서간 토론 프로토콜 (1: Discussion 멀티라운드 활성화)
ENABLE_DISCUSSION_PROTOCOL=1

# 태스크 의존성 자동 디스패치 (1: 의존 태스크 자동 처리)
ENABLE_AUTO_DISPATCH=1

# Codex↔Claude Code 교차 검증 (1: 결과를 서로 교차 검증)
ENABLE_CROSS_VERIFICATION=1

# PM 목표 달성 루프 GoalTracker (1: 권장, 목표 추적 활성화)
ENABLE_GOAL_TRACKER=1

# =============================================================================
# [자율 에이전트] Autonomous Mode
# =============================================================================

# 자율 실행 모드 (true: 사용자 입력 없이 인터랙티브 스킬 자동 응답)
# 사용 예: AUTONOMOUS_MODE=true claude --dangerously-skip-permissions "..."
AUTONOMOUS_MODE=false

# =============================================================================
# [DB/스토리지] 데이터베이스 및 파일 경로
# =============================================================================

# Context DB 경로 (선택, 기본값: ~/.ai-org/context.db)
CONTEXT_DB_PATH=~/.ai-org/context.db

# 공유 메모리 JSON 경로 (선택, 기본값: ~/.ai-org/shared_memory.json)
SHARED_MEMORY_PATH=~/.ai-org/shared_memory.json

# 리포트 저장 디렉토리 (선택, 기본값: ./reports)
AIORG_REPORT_DIR=./reports

# 산출물·데이터 저장 루트 (선택, 기본값: ~/telegram-ai-org-data)
# telegram-ai-org 오픈소스 코드와 분리된 외부 디렉토리에 모든 산출물을 저장한다.
# - 생성 코드, PRD, 리서치 보고서, 스킬 로그 등 운영 산출물 일체
# - ~/.ai-org/workspace 는 더 이상 신규 산출물에 사용하지 않는다 (기존 내용 보존)
# 오픈소스 사용자: 자신의 경로로 변경 (예: ~/my-aiorg-data)
AI_ORG_DATA_DIR=~/telegram-ai-org-data

# MCP 서버용 DB 경로 (선택, 기본값: ~/.ai-org/context.db)
DB_PATH=~/.ai-org/context.db

# MCP 서버 봇 식별자 (선택)
BOT_ID=unknown

# =============================================================================
# [봇 동작] 타임아웃 및 하트비트 설정
# =============================================================================

# 봇 무응답 타임아웃 (선택, 초 단위, 기본값: 120)
BOT_IDLE_TIMEOUT_SEC=300

# 하트비트 전송 간격 (선택, 초 단위, 기본값: 30)
BOT_HB_INTERVAL_SEC=30

# 봇 절대 상한 타임아웃 (선택, 초 단위, 기본값: 1800 = 30분)
BOT_MAX_TIMEOUT_SEC=1800

# 태스크 실행 절대 상한 타임아웃 (선택, 초 단위, 기본값: 1800 = 30분)
# telegram_relay.py의 TASK_EXEC_TOTAL_TIMEOUT_SEC 참조
TASK_EXEC_TOTAL_TIMEOUT_SEC=1800

# PM 채팅 응답 대기 타임아웃 (선택, 초 단위, 기본값: 300)
PM_CHAT_REPLY_TIMEOUT_SEC=300

# 동시 실행 PM 봇 수 (선택, 기본값: 1)
PM_COUNT=1

# 조직 이름 식별자 (선택, 기본값: global)
PM_ORG_NAME=global

# =============================================================================
# [비용/성능 제한] API 비용 및 서킷 브레이커
# =============================================================================

# 시간당 API 호출 상한 (선택, 기본값: 100)
PM_HOURLY_CALL_LIMIT=40

# 일일 비용 상한 USD (선택, 기본값: 50.0)
DAILY_COST_LIMIT_USD=50.0

# 토큰 1000개당 비용 USD (선택, 기본값: 0.003)
COST_PER_1K_TOKENS_USD=0.003

# 서킷 브레이커 오류 임계치 (선택, 기본값: 3)
CIRCUIT_BREAKER_ERROR_THRESHOLD=3

# 서킷 브레이커 리셋 시간 (선택, 초 단위, 기본값: 600)
CIRCUIT_BREAKER_RESET_SEC=300

# =============================================================================
# [Staleness 체크] 태스크 신선도 모니터링
# =============================================================================

# 스탈 임계값 (선택, 초 단위, 기본값: 300)
STALE_THRESHOLD_SEC=300

# 하트비트 그레이스 시간 (선택, 초 단위, 기본값: 120)
HEARTBEAT_GRACE_SEC=120

# 서브태스크 타임아웃 (선택, 초 단위, 기본값: 600)
SUBTASK_TIMEOUT_SEC=600

# 스탈 체크 인터벌 (선택, 초 단위, 기본값: 60)
STALENESS_CHECK_INTERVAL_SEC=60

# =============================================================================
# [컨텍스트 윈도우] LLM 컨텍스트 크기 제한
# =============================================================================

# 히스토리 최대 메시지 수 (선택, 기본값: 20)
MAX_HISTORY_MESSAGES=20

# 히스토리 최대 토큰 수 (선택, 기본값: 6000)
MAX_HISTORY_TOKENS=6000

# =============================================================================
# [PM 오케스트레이터] 태스크 동시성 제한
# =============================================================================

# 재작업 최대 재시도 횟수 (선택, 기본값: 2)
MAX_REWORK_RETRIES=2

# 동시 부모 태스크 최대 수 (선택, 기본값: 10)
MAX_CONCURRENT_PARENT_TASKS=10

# Claim TTL (선택, 초 단위, 기본값: 600 = 10분)
CLAIM_TTL_SEC=600

# 텍스트 해시 중복 방지 TTL (선택, 초 단위, 기본값: 86400 = 24시간)
TEXT_HASH_TTL_SEC=86400

# =============================================================================
# [첨부파일 분석] 비전 브리지 (선택 기능)
# =============================================================================

# 이미지 분석 브리지 명령어 (선택)
ATTACHMENT_VISION_BRIDGE_CMD=

# 멀티모달 브리지 명령어 (선택)
ATTACHMENT_MULTIMODAL_BRIDGE_CMD=

# 비전 브리지 타임아웃 (선택, 초 단위, 기본값: 60)
ATTACHMENT_VISION_BRIDGE_TIMEOUT_SEC=60

# =============================================================================
# [로깅] 로그 레벨 및 디버그 설정
# =============================================================================

# 로그 레벨 (선택, 기본값: INFO)
# 선택 가능한 값: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# 디버그 모드 (선택, false=비활성화)
# true 시 상세 트레이스, 원시 API 요청/응답 출력
AIORG_DEBUG=false

# =============================================================================
# [성장실] 인프라 베이스라인 버전
# 실험 로그(self-improve-history.jsonl, iter-loop-history.jsonl)에 자동 주입되는
# 인프라 버전 태그. 지표 이상치 발생 시 인프라 변경 여부를 즉시 연결할 수 있다.
# 참조 소스: infra/infra-baseline.yaml 의 version 필드 (예: "1.0.0")
# 기본값: "unknown" (미설정 시)
# =============================================================================
INFRA_BASELINE_VERSION=

# =============================================================================
# [디스플레이] UI 설정
# =============================================================================

# 메시지 디스플레이 제한기 활성화 (선택, true/false, 기본값: true)
USE_DISPLAY_LIMITER=true

# 터미널 색상 비활성화 (선택, 값이 있으면 색상 off)
# NO_COLOR=1

# =============================================================================
# [E2E 테스트 전용] Telegram MTProto API (일반 운영 시 불필요)
# my.telegram.org 에서 발급
# =============================================================================

# Telegram API ID (E2E 테스트 스크립트 전용)
TELEGRAM_API_ID=

# Telegram API Hash (E2E 테스트 스크립트 전용)
TELEGRAM_API_HASH=

# 등록된 전화번호 (E2E 테스트 스크립트 전용)
TELEGRAM_PHONE=

# =============================================================================
# [Phase 2 이식] Tool Registry / Platform Adapter 기능 플래그
# core/tool_registry.py, core/platform_adapter.py 참조
# 기본값: false (안전 우선 — 활성화 시 true 로 변경)
# =============================================================================

# Hermes 패턴 이식 통합 플래그 (1: 아래 3개 Hermes 기능 전체 활성화)
# NousResearch hermes-agent 패턴 이식: Tool Registry·Platform Adapter·Context Compressor
ENABLE_HERMES_FEATURES=1

# Tool Registry 활성화 (hermes-agent 이식)
# 1 로 설정하면 core/tool_registry.py 의 중앙 집중식 도구 레지스트리가 활성화됨.
ENABLE_TOOL_REGISTRY=1

# Platform Adapter 활성화 (hermes-agent 이식)
# 1 로 설정하면 core/platform_adapter.py 의 플랫폼 추상화 레이어가 활성화됨.
ENABLE_PLATFORM_ADAPTER=1

# Context Compressor 활성화 (hermes-agent 이식)
# 1 로 설정하면 core/context_compressor.py 의 컨텍스트 압축 레이어가 활성화됨.
ENABLE_CONTEXT_COMPRESSOR=1

# =============================================================================
# [Phase 3-A] REST API 고급 기능 플래그
# core/api/rate_limiter.py, core/api/metrics.py, core/api/audit_log.py 참조
# 기본값: false (안전 우선 — 활성화 시 true 로 변경)
# =============================================================================

# REST API 활성화 (1: FastAPI 라우터 전체 활성화)
ENABLE_REST_API=1

# Rate Limiting 활성화 (슬라이딩 윈도우 기반 IP/API Key 요청 제한)
# 1 로 설정하면 RATE_LIMIT_REQUESTS / RATE_LIMIT_WINDOW_SECONDS 기준 적용
ENABLE_RATE_LIMITING=1

# Rate Limit 설정 (ENABLE_RATE_LIMITING=1 시 적용)
# RATE_LIMIT_REQUESTS: 윈도우 내 허용 최대 요청 수 (기본값: 60)
# RATE_LIMIT_WINDOW_SECONDS: 슬라이딩 윈도우 크기 (기본값: 60초)
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW_SECONDS=60

# Audit Log 활성화 (태스크 생성/삭제/404 이벤트를 JSONL 로 기록)
# 1 로 설정하면 AIMESH_AUDIT_LOG_PATH 에 지정된 파일에 로그 기재
ENABLE_AUDIT_LOG=1

# Audit Log 파일 경로 (선택, 기본값: data/audit.log)
AIMESH_AUDIT_LOG_PATH=data/audit.log

# AIMesh SQLite DB 경로 (선택, 기본값: data/tasks.db)
AIMESH_DB_PATH=data/tasks.db

# =============================================================================
# [Phase 2a] Repository / Service 계층 기능 플래그
# core/repositories/, core/services/ 참조
# =============================================================================

# Repository 패턴 활성화 (1: SQLite 기반 태스크/메시지/봇상태 저장)
ENABLE_REPOSITORY_PATTERN=1

# Dispatch Service 활성화 (1: 디스패치 비즈니스 서비스 계층)
ENABLE_DISPATCH_SERVICE=1

# Orchestration Service 활성화 (1: 오케스트레이션 비즈니스 서비스 계층)
ENABLE_ORCHESTRATION_SERVICE=1

# API 인증 활성화
# 주의: AIMESH_API_KEYS 미설정 시 1로 변경하면 모든 API 접근이 차단됨 — 기본 비활성화 유지
ENABLE_API_AUTH=0
# AIMESH_API_KEYS=key-abc123,key-def456

# =============================================================================
# [평가 컨텍스트] 봇 자기인식 — 평가 중임을 인지하도록 시스템 프롬프트 주입
# core/eval_context.py 참조
# =============================================================================

# 평가 컨텍스트 주입 활성화 (1: 봇이 평가 중임을 시스템 프롬프트에서 인지)
ENABLE_EVAL_CONTEXT=1

# 상시 평가 인지 모드 (1: EVAL_SESSION 없이도 항상 평가 컨텍스트 주입)
# 운영 환경에서 봇이 항상 평가 기준을 인지하도록 할 때 사용
EVAL_ALWAYS=1

# 세션별 평가 활성화 (1: 특정 세션만 평가 대상으로 지정할 때)
# EVAL_ALWAYS=1이면 이 변수는 불필요
# EVAL_SESSION=1

# =============================================================================
# [테스트] E2E 멀티봇 라우팅 테스트용 봇 토큰
# tests/e2e/test_pm_dispatch_e2e.py 에서 사용
# BotFather에서 테스트 전용 봇을 생성하고 토큰을 입력하세요.
# =============================================================================
TEST_BOT_TOKEN_PM=
TEST_BOT_TOKEN_PRODUCT=
TEST_BOT_TOKEN_ENGINEERING=
