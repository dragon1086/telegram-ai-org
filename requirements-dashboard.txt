# =============================================================================
# requirements-dashboard.txt — 대시보드 전용 최소 의존성
#
# 전체 봇 시스템 없이 대시보드만 실행할 때 사용한다.
#
# 설치:
#     pip install -r requirements-dashboard.txt
#
# 실행:
#     python dashboard.py
#     uvicorn dashboard:app --port 8080
# =============================================================================

# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi>=0.100                   # 0.115.x — async REST API + SSE 스트리밍
uvicorn[standard]>=0.20          # 0.34.x — ASGI 서버 (websockets, httptools 포함)

# ─── Database ────────────────────────────────────────────────────────────────
aiosqlite>=0.19                  # 0.22.x — async SQLite (태스크 DB 폴링)

# ─── Configuration ───────────────────────────────────────────────────────────
python-dotenv>=1.0               # .env 로드 — AIMESH_DB_PATH, DASHBOARD_PORT 등
PyYAML>=6.0                      # YAML 설정 파일 파싱

# ─── Logging ─────────────────────────────────────────────────────────────────
loguru>=0.7                      # 구조화 로그 (콘솔 + 파일 rotation)
